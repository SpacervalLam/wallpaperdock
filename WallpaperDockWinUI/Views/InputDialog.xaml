<Window
    x:Class="WallpaperDockWinUI.Views.InputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WallpaperDockWinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="">

    <Border x:Name="RootGrid" Padding="28" MinWidth="480" MinHeight="220" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="12" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1">
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="DialogTitle" FontSize="20" FontWeight="SemiBold" Text="重命名" Margin="0,0,0,16" />

            <TextBox x:Name="InputBox" Grid.Row="1" Height="36" Margin="0,0,0,12" HorizontalAlignment="Stretch" />

            <TextBlock x:Name="ErrorText" Grid.Row="2" Foreground="Red" Visibility="Collapsed" TextWrapping="Wrap" />

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="CancelButton" Content="取消" Margin="0,0,8,0" MinWidth="80" />
                <Button x:Name="OkButton" Content="保存" MinWidth="80" Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>