<UserControl
    x:Class="WallpaperDockWinUI.Views.ToastNotification"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WallpaperDockWinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="300">
    <Grid Background="#333333" CornerRadius="8" Padding="16" HorizontalAlignment="Right" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Grid.Row="0" Text="错误" FontSize="16" FontWeight="Medium" Foreground="White" Margin="0,0,0,8" />
        <TextBlock x:Name="MessageText" Grid.Row="1" Text="语音识别启动失败" FontSize="14" Foreground="#CCCCCC" Margin="0,0,0,12" TextWrapping="Wrap" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <TextBlock Text="点击此处打开语音隐私设置" FontSize="14" Foreground="#0078D7" Tapped="SpeechPrivacyLink_Tapped" />
            <TextBlock Text="|" FontSize="14" Foreground="#CCCCCC" />
            <TextBlock Text="点击此处打开麦克风隐私设置" FontSize="14" Foreground="#0078D7" Tapped="MicrophonePrivacyLink_Tapped" />
        </StackPanel>
    </Grid>
</UserControl>